# Minimal dependencies. No provider SDKs, raw HTTP via requests instead.
python-dotenv>=1.0
requests>=2.31
Pillow>=10.0
PyMuPDF>=1.24
lxml>=5.0

# Evaluation module aep_eval (ADR-006): manifest and result schemas are
# checked with jsonschema; rapidfuzz supplies Levenshtein distance and the
# matching edit opcodes for the fidelity/scope decomposition.
jsonschema>=4.0
rapidfuzz>=3.0

# Development: runnable checks for the pipeline data contract (tests/)
pytest>=8.0
